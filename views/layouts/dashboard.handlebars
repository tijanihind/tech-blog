<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tijani Tech Block</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>

  <body>

    <header class="header">

      <nav class="nav">
        <a href="/">Home</a>
        <a href="/dashboard">Dashboard</a>
        {{#if loggedIn}}
          <a class="link" href="#" id="logout-link">Logout</a>
        {{else}}
          <a class="link" href="/login">Login</a>
        {{/if}}
      </nav>

      <h1 class="app-title"><a href="/">Your Dashboard</a></h1>
    </header>
    <main class="">
      {{{body}}}
    </main>

    <footer>

    </footer>

    {{#if loggedIn}}
      <script src="/js/logout.js"></script>
    {{/if}}
  </body>

</html>